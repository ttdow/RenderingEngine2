#pragma once

#include "../Vector3.h"

namespace Engine
{
	class ParticleSystemData3
	{
	public:

		/*
		ParticleSystemData3();
		ParticleSystemData3(size_t numOfParticles);
		virtual ~ParticleSystemData3();

		void Resize(size_t newNumberOfParticles);
		size_t NumberOfParticles() const;

		const Vector3* const Positions() const;
		const Vector3* const Velocities() const;
		const Vector3* const Forces() const;

		void AddParticle(const Vector3& newPosition, const Vector3& newVelocity = Vector3(), const Vector3& newForce = Vector3());
		void AddParticles(const std::vector<Vector3>& newPositions, const std::vector<Vector3>& newVelocities = std::vector<Vector3>(), const std::vector<Vector3>& newForces = std::vector<Vector3>());

	private:

		std::vector<Vector3> positions;
		std::vector<Vector3> velocities;
		std::vector<Vector3> forces;
		*/
	};
}